import { Container, Wrapper } from './spinner.styles';
import './Spinner.styles.scss'
function Spinner() {
    return (
        <Wrapper>
            <Container />
        </Wrapper>
    );
}

export default Spinner;